# Exemplo de configuração .env para instalação independente na Hostinger
# 
# Este arquivo serve como referência para deploy em hostings compartilhados
# onde a aplicação roda em modo single-tenant (uma loja por instalação).
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env na raiz do projeto (public_html/.env)
# 2. Preencha os valores reais (especialmente DB_*)
# 3. NUNCA commite o arquivo .env no Git (já está no .gitignore)

# ============================================
# AMBIENTE
# ============================================
APP_ENV=production
APP_DEBUG=false
APP_URL=https://pontodogolfeoutlet.com.br

# ============================================
# MODO DE OPERAÇÃO
# ============================================
# Para instalações independentes (uma loja por servidor), use "single"
# Para instalações multi-tenant (múltiplas lojas), use "multi"
APP_MODE=single

# Tenant ID fixo para modo single (geralmente 1)
DEFAULT_TENANT_ID=1

# ============================================
# BANCO DE DADOS REMOTO
# ============================================
# IMPORTANTE: Substitua pelos valores reais do seu banco
DB_HOST=srv1075.hstgr.io
DB_PORT=3306
DB_NAME=u426126796_pontodogolpe
DB_USER=u426126796_pontodogolfe
DB_PASS=SUA_SENHA_AQUI

# ============================================
# SESSÃO
# ============================================
SESSION_NAME=ECOMMERCE_SESSION

# ============================================
# NOTAS
# ============================================
# - O banco de dados pode ser o mesmo para dev e produção
# - Em modo single, o sistema usa sempre o tenant_id especificado em DEFAULT_TENANT_ID
# - O domínio não precisa estar cadastrado em tenant_domains quando APP_MODE=single
# - Para modo multi, é necessário cadastrar domínios em tenant_domains

